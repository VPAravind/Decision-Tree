# Decision-Tree
Decision Tree implementation on the WaitTable.csv, toy data set. The target attribute is a Wait attribute.

The implementation uses only entropy and doesn not use information gain to implement the decision tree. The program uses Pandas package and implements the tree using recursion with 0 entropy being the base case.  
